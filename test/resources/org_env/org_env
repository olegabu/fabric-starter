export ORG=${ORG}
export DOMAIN=${DOMAIN}
#--------------------------------------------
export MY_IP=${MY_IP}
export API_PORT=4000
#--------------------------------------------
export BOOTSTRAP_IP=${BOOTSTRAP_IP}
export BOOTSTRAP_API_PORT=${BOOTSTRAP_API_PORT}
export BOOTSTRAP_ORG_DOMAIN=
export BOOTSTRAP_SERVICE_URL=${BOOTSTRAP_SERVICE_URL}
#--------------------------------------------
export ORDERER_TYPE=${ORDERER_TYPE}
export ORDERER_PORT=${ORDERER_PORT}
export ORDERER_WWW_PORT=${ORDERER_WWW_PORT}
#export ORDERER_DOMAIN=osn-${ORG}.${DOMAIN}
#export ORDERER_GENERAL_LISTENPORT=7050
export ORDERER_NAME=${ORDERER_NAME}
#--------------------------------------------
export ENROLL_SECRET=${ENROLL_SECRET}
#--------------------------------------------
export API_PORT=${API_PORT}
export WWW_PORT=${WWW_PORT}
export CA_PORT=${CA_PORT}
export PEER0_PORT=${PEER0_PORT}
export LDAP_PORT_HTTP=${LDAP_PORT_HTTP}
export LDAP_PORT_HTTPS=${LDAP_PORT_HTTPS}
#--------------------------------------------
export FABRIC_STARTER_HOME=${FABRIC_STARTER_HOME}