{
  "consenters": [
    {
      "client_tls_cert": "${RAFT_NEWORDERER_CLIENT_TLS_CERT}",
      "host": "${RAFT_NEWORDERER_HOST}",
      "port": "${RAFT_NEWORDERER_PORT}",
      "server_tls_cert": "${RAFT_NEWORDERER_SERVER_TLS_CERT}"
    }
  ]
}